import { TitleBlock } from "@/components/Shared";
import { User } from "lucide-react";

export const Presenciales = () => {
  return (
    <div className="p-6">
      <TitleBlock title="Alumnos" icon={User} />
    </div>
  );
};
